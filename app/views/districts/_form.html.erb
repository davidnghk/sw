<div class="container">
	<%= simple_form_for(@district) do |f| %>

	  <div class="form-inputs">
	  	<%= f.input :code %>
	    <%= f.input :name %>
	    <%= f.input :chi_name %>
	  </div>

	  <div class="form-actions">
	    <%= f.button :submit %>
	  </div>
	<% end %>
</div>